apiVersion: v1
kind: Service
metadata:
  name: wifi-ui-service
spec:
  type: NodePort
  selector:
    app: wifi-ui
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30008
